# 代理池订阅配置文件
# 每行一个订阅 URL，# 开头为注释
# 修改此文件后会自动触发更新（需要 proxy-pool-watcher 服务）
#
# 支持格式：Clash YAML / Base64 URI 列表
# 多个订阅的节点会合并到同一个端口池
#
# 示例：
# https://example.com/sub1/clash
# https://example.com/sub2/clash
